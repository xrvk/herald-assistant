# Scout Report — cp .env.example .env and fill in your values.
# See SETUP.md for detailed setup instructions.

# ── Calendars (at least one required) ── see SETUP.md §1
# Add calendars using numbered slots. Any ICS URL works (iCloud, Outlook, Google, etc.).
# Labels are your choice — they appear in digests, LLM context, and WORK_LABELS matching.
# CALENDAR_1_URL=webcal://p60-caldav.icloud.com/published/2/YOUR_ID
# CALENDAR_1_LABEL=Personal
# CALENDAR_2_URL=https://outlook.office365.com/owa/calendar/YOUR_ID/calendar.ics
# CALENDAR_2_LABEL=Work
# CALENDAR_3_URL=https://calendar.google.com/calendar/ical/YOUR_ID/basic.ics
# CALENDAR_3_LABEL=Family
#
# Up to 9 numbered slots (CALENDAR_1 through CALENDAR_9).

# ── Discord Bot (required for interactive chat) ── see SETUP.md Step 2
# Omit only if you want notification-only mode (requires APPRISE_URL + a schedule).
# DISCORD_BOT_TOKEN=
# DISCORD_CHANNEL_ID=
# Security: bot only responds to these user IDs. If unset, all users can interact.
# To find your user ID: Discord → Settings → Advanced → enable Developer Mode,
# then right-click your name → Copy User ID. See SETUP.md Step 2.
# DISCORD_ALLOWED_USERS=123456789012345678,987654321098765432

# ── Notifications (required for scheduled digests) ── see SETUP.md §3
# Discord webhook for scheduled digests (convert webhook URL to Apprise format).
# https://discord.com/api/webhooks/ID/TOKEN → discord://ID/TOKEN
# APPRISE_URL=discord://webhook_id/webhook_token
# Digests are controlled by WEEKNIGHT_SCHEDULE and WEEKEND_SCHEDULE below.

# ── LLM Backend ── see SETUP.md §4
# "gemini" (cloud, recommended — free tier, no GPU) or "ollama" (local, private)
# ⚠ Requires GEMINI_API_KEY below (or change to ollama)
LLM_BACKEND=gemini

# Gemini — get a free key: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_api_key_here
# GEMINI_MODEL=gemini-2.5-flash-lite

# Ollama (optional — local inference, requires Ollama running on the host)
# LLM_BACKEND=ollama
# OLLAMA_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=gemma4:e4b
# Tip: set GEMINI_API_KEY alongside LLM_BACKEND=ollama to get automatic Gemini
# flash-lite fallback whenever Ollama is unreachable.

# ── Optional (sensible defaults) ──

# TZ=America/Los_Angeles

# Scheduled digests: Format: "days HH:MM", just "HH:MM" (uses default days), just days (uses default time), or "off"
# Days: mon,tue,wed,thu,fri,sat,sun
# Uncomment and set APPRISE_URL above to enable scheduled digests, then change
# these to your desired schedule. They are OFF by default.
WEEKNIGHT_SCHEDULE=off
WEEKEND_SCHEDULE=off
# ⚠ REQUIRED if you enable WEEKNIGHT_SCHEDULE — set to your work calendar label(s).
# Must match your CALENDAR_N_LABEL values. If unset, weeknight digests show "No meetings."
# WORK_LABELS=Work

# Hide events from digests and LLM (comma-separated substrings, case-insensitive)
# IGNORED_EVENTS=pick up kids,lunch

# CONTEXT_DAYS=7              # days of future events the LLM can see
# HISTORY_DAYS=10             # days of past events for history questions (0=off)
# HISTORY_CACHE_TTL=21600     # past-event cache in seconds (6h)
# CACHE_TTL=3600              # calendar fetch cache in seconds (1h)

# Conversation memory — enables follow-up questions
# CONV_HISTORY_TURNS=3        # Q+A pairs kept per user
# CONV_HISTORY_TTL=1800       # staleness timeout in seconds (30min)
# CONV_HISTORY_CTX_BUMP=4096  # extra Ollama context tokens (no effect on Gemini)

# Override the LLM personality (calendar data is appended automatically)
# SYSTEM_PROMPT=You are Kalinda, a sarcastic but helpful calendar assistant.
